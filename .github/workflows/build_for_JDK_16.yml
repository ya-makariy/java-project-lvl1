name: ci for build

on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set up JDK 16
        uses: actions/setup-java@v2
        with:
          java-version: '16'
          distribution: 'adopt'
      - name: make-build
        run: make build